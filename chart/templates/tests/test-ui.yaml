{{- include "bb-test-lib.cypress-configmap.overrides" (list . "minio-test.cypress-configmap") }}
{{- define "minio-test.cypress-configmap" }}
metadata:
  labels:
    {{- include "minio.labels" . | nindent 4 }}
{{- end }}
---
{{- include "bb-test-lib.cypress-runner.overrides" (list . "minio-test.cypress-runner") -}}
{{- define "minio-test.cypress-runner" -}}
metadata:
  labels:
    {{- include "minio.labels" . | nindent 4 }}
{{- end }}
